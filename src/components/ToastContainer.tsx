export function ToastContainer() {
  // Toasts render in the bottom status bar (StatusBarToasts). Keep this
  // mount for backwards-compatible imports; it intentionally renders nothing.
  return null;
}
