# Config repair with backup
Before editing a config file: copy it under /tmp/tw-ai-backup, apply the change, then validate
(nginx -t / sshd -t / etc). Keep rollback path ready. Skill is guidance only.
