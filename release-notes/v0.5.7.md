# v0.5.7 — Windows Git Bash 本地终端

## Windows 本地终端

- 本地会话 **仅使用 Git Bash**（Git for Windows）；未检测到则侧栏提示安装，无法打开 PowerShell 回退
- Git Bash 下：`~`、彩色 `ls`、目录点击、Find、文件预览（经 `cygpath` 访问 Windows 路径）

## 界面

- 书签首项：**Git Bash 本地终端**（单行标题）
- 无打开会话时隐藏顶部标签栏；首页「快速开始」与简介同步更新
- 深色 UI：近黑色 OS 图标（如 Apple）自动提亮
