# v0.6.2 — 修复选区与右键交互

## 修复

- **选区后右键页签**：拖选或全选终端文字后，页签右键一次即可打开菜单，无需点两次
- **选区后右键误触左键**：修复拖选后右键被当成左键（切换页签等）的问题
- **文件路径右键**：右键不再打开文件预览，仅左键预览；右键保留复制菜单
- **浏览器右键菜单**：屏蔽 WebView 默认菜单，保留页签/快捷目录等应用菜单；终端区仍可用系统复制菜单
- **跨平台**：Windows / macOS / Linux 统一基于 DOM 标准事件处理；macOS 触控板 secondary click 单独加固
