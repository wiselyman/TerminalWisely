# v0.5.11 — 内置终端等宽字体

## 修复

- Linux / Ubuntu 上英文仍显示为细、发虚的系统默认 `monospace`：v0.5.10 仅改字体名，未打包字体文件
- 应用内置 **JetBrains Mono** 与 **Noto Sans Mono**（`@fontsource`），终端打开前通过 `document.fonts.load` 预加载，确保 xterm canvas 使用正确字形
