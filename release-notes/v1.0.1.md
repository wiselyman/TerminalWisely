**用自然语言修 Linux。** 跨平台桌面终端 + **AI Linux Engineer**。

## 更新内容（v1.0.1）

### 修复
- **AI 引擎自理**：发行包内嵌独立 Python；首次打开自动创建私有环境并安装依赖，**用户无需手动 pip**
- 启动过程在面板显示进度（准备环境 / 安装依赖 / 启动）
- 修正 DMG 安装后因系统 Python 缺 uvicorn 导致 `/health` 失败的问题

### 说明
- 首次启动 AI 可能需 1–3 分钟下载依赖，请保持网络畅通；之后秒开

---

## 更新内容（v1.0.0）

### 重磅：AI Linux Engineer
- 在已连接的本地/SSH 主机上，用自然语言描述问题
- 能力策略引擎（R0–R4）、批准 UI、网络变更定时回滚
