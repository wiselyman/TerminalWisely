# v0.5.12 — 修复 Linux ARM64 终端黑屏

## 修复

- v0.5.11 在 xterm 打开前等待 `document.fonts.load` / `document.fonts.ready`；在 Ubuntu WebKitGTK（含 ARM64）上可能挂起或 reject，终端从未挂载，界面只剩黑底
- 终端立即初始化；字体预加载改为超时 + `allSettled`，失败时回退系统字体并 refresh
