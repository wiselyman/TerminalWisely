# v0.5.13 — 修复 Linux 窗口全黑

## 修复

- WebKitGTK 在部分 Linux GPU 环境（ARM64 Mali、NVIDIA 等）下 DMABUF 渲染失败，窗口打开但 WebView 内容不显示
- 应用启动前自动设置 `WEBKIT_DISABLE_DMABUF_RENDERER=1`（Tauri 官方 workaround）

## 说明

这与 v0.5.12 的字体预加载无关；x64 正常而 ARM64 全黑，多为显卡/WebKit 渲染路径差异，而非前端 JS 架构问题。
