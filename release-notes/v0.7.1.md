# v0.7.1 — 文件右键菜单与路径解析修复

## 修复

- **文件右键菜单**：靠近终端底部时改为向上弹出，避免菜单被窗口裁切
- **重命名/删除路径**：从提示符（如 `wiselyman@host:~/下载$`）正确解析当前目录，修复在子目录下操作报 `No such file`
- **zsh 命令解析**：修复 `extractCommandLine` 误把 cwd 当成命令的问题
