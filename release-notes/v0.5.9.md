# v0.5.9 — 终端链接、sudo 预览、磁盘 IO 与布局适配

## 新增

- 服务器资源面板显示磁盘读/写速度与累计 IO

## 改进

- 终端路径链接仅对 `ls` / `ls -l` 列表项生效，减少误识别
- 打开预览、侧栏或右侧工具面板时终端自动调整列宽

## 修复

- SSH 预览/保存 `/etc` 等需 root 权限的文件时 permission denied；弹出 sudo 密码框后可通过 `sudo cat` / `sudo tee` 读写
