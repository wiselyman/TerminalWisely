跨平台桌面终端：本地 Git Bash + SSH/SFTP。

## 更新内容（v0.7.0）

### 新增
- **文件右键菜单**：`ls` 列表中重命名、删除、移动到目录；复制文件名称与路径
- **跨服务器传输**：SCP 源机直传（书签密码自动注入）
- **目录打包下载**：Ctrl/Cmd+点击目录名下载 `.tar.gz`

### 改进
- **SSH 连接加速**：认证后立即开 Shell，探测与历史写入改后台；20s 超时、自动重试、keepalive
- **Find**：自动同步 cwd，路径 Tab 补全；传输进度显示目标路径；链接悬停下划线

### 修复
- 文件操作对话框按钮大小；右键 `No such file` 误报 toast；界面渲染 `TERMINAL_FOCUS_EVENT` 错误
- 跨服传输进度、目录链接下划线等

---

## 更新内容（v0.6.3）

### 改进
- **应用图标**：全新 `W_` 品牌标识（蓝色 W + 绿色终端光标），简洁识别 Terminal**W**isely

### 修复
- **多页签切换**：多个 SSH/本地页签并存时，点击页签可正常切换会话

### 文档
- README 同步 v0.6.x 功能；发布清单增加 README 版本更新

---

## 更新内容（v0.6.2）

### 修复
- **选区与右键**：终端选中文本后，页签右键一次出菜单；不再误触发左键切换页签
- **文件路径**：右键路径只出复制菜单，左键才预览/进目录
- **应用菜单**：屏蔽浏览器/WebView 默认右键，保留页签菜单、快捷目录菜单与表单右键
- **跨平台**：Windows / macOS / Linux 一致；macOS 触控板 secondary click 已单独处理

---

## 更新内容（v0.6.1）

### 修复
- **macOS 终端鼠标乱码**：远程程序（vim、htop 等）开启鼠标模式后未正确关闭时，移动鼠标会向命令行持续注入 `35;70;43M` 类字符；现于打开/聚焦/切换标签/SSH 断开时自动重置鼠标追踪

---

## 更新内容（v0.6.0）

### 新增
- **命令导航**：贴边工具栏打开，内置 90+ 条 Linux 运维命令；填参数后插入终端（不自动执行）
- 分类覆盖服务、日志、磁盘、进程、**网络/防火墙/端口**、软件包（apt/dnf/apk/zypper/pacman）、文件、用户、计划任务、系统信息
- 支持自定义命令；按 SSH 会话发行版自动筛选

### 改进
- **场景化参数向导**：服务名下拉、路径 Tab 补全、进程选择、chmod 权限场景、chown 用户/组选择、搜索关键词预设、常见端口点选
- 命令面板浮层显示，不挤压终端布局

---

## 更新内容（v0.5.13）

### 修复
- **Linux 窗口全黑**：启动时禁用 WebKitGTK DMABUF 渲染路径，修复部分 ARM64 / NVIDIA 等环境下 WebView 不显示内容的问题

---

## 更新内容（v0.5.12）

### 修复
- **Linux ARM64 黑屏**：v0.5.11 字体预加载在 WebKitGTK 上可能挂起/失败，导致 xterm 从未初始化；改为先打开终端、字体加载失败也不阻塞

---

## 更新内容（v0.5.11）

### 修复
- **Linux 终端英文发虚**：内置 JetBrains Mono / Noto Sans Mono，不再依赖系统字体；xterm 启动前预加载字体

---

## 更新内容（v0.5.10）

### 改进
- **Linux 终端字体**：按操作系统选择等宽字体栈（Ubuntu Mono、Noto Sans Mono CJK 等），中英文显示更一致
- **预览面板**：等宽字体与终端统一，并优化行高与抗锯齿

---

## 更新内容（v0.5.9）

### 改进
- **终端链接**：仅 `ls` / `ls -l` 列出的文件名可点击，减少登录信息、docker 输出等误识别
- **文件预览**：SSH 上无权限的系统文件（如 `/etc/...`）可输入 sudo 密码后预览与保存
- **服务器资源**：面板新增磁盘读/写速度与累计 IO
- **布局**：侧栏折叠、预览/右侧面板开关或调宽时，终端自动 refit 适配列数

---

## 更新内容（v0.5.8）

### 修复
- **macOS / Linux 构建**：修复非 Windows 平台引用 `windows_cd_and_list_command` 导致 CI 与 Release 编译失败

---

## 更新内容（v0.5.7）

### Windows 本地终端
- **仅 Git Bash**：Windows 本地会话统一使用 Git for Windows 自带的 Git Bash；未安装时提示安装，不再回退 PowerShell / MSYS2 / WSL
- **路径与 Find**：Git Bash 下支持 `~`、彩色 `ls`、目录点击、Find 与文件预览（`cygpath` 转换）

### 界面
- **书签**：本地项显示「Git Bash 本地终端」，去掉重复副标题
- **首页**：无会话时隐藏顶部标签栏；更新「快速开始」与产品介绍
- **深色主题**：macOS Apple 等近黑品牌图标自动提亮，避免与背景融为一体

---

## 更新内容（v0.5.6）

### 本地终端
- **页签与书签 OS 图标**：Windows / macOS（Apple）/ Linux 区分显示；书签首项固定「Windows/macOS/Linux 本地终端」
- **「+」仅新建 SSH**：本地入口已在书签，点击 + 直接打开远程连接表单
- **Windows `~` 目录**：修复 `cd %USERPROFILE%` 失败；改用绝对路径与 cmd/PowerShell 正确语法
- **macOS 连续点击同级目录**：修复路径解析错误导致「路径不存在」

### 界面
- **Windows 图标**：修正四格窗标变形
- **macOS SSH 表单**：认证方式下拉框高度与输入框对齐

---

## 更新内容（v0.5.5）

### 本地终端（macOS / Linux / Windows）
- **路径与 cwd**：本地会话跟踪当前目录；点击 `ls` 条目可 cd / 预览；Find 从当前目录搜索
- **macOS 彩色 ls**：启动时注入 `CLICOLOR=1` 等环境变量；`ls -l` 识别权限位后的 `@` / `+` 标记
- **可点击路径**：终端链接显示下划线，悬停更易辨认

### 跨会话传文件
- **本地 → SSH**：Shift+点击文件路径或 Ctrl/Cmd+拖拽到 SSH 标签，可发送到其他服务器（本地上传/下载仍仅 SSH）

### Find 与预览
- 打开/关闭预览时 **Find 保持打开**；预览与 Find 并排显示，不再互相遮挡

---

## 更新内容（v0.5.4）

### 连接与预览
- **书签**：未填连接名称时自动以 **IP/主机** 保存；相同 host+port+用户会更新已有书签，不再重复
- **Find → 预览**：点击搜索结果打开文件时自动关闭 Find，预览/编辑不再被遮挡

### 任务管理器（本地 + SSH）
- **本地标签**（macOS / Linux / Windows）与 SSH 一致：先显示进程列表，再解析端口；骨架屏与列头 spinner
- **轮询**：CPU/内存 2 秒、端口 8 秒分开刷新；修复端口刷新长期被阻塞的问题

---
